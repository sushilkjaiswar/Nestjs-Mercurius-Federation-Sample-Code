type Post @key(fields: "id") {
  id: ID!
  title: String!
  body: String!
  user: User
}

extend type User @key(fields: "id") {
  id: ID! @external
  name: String! @external
}

extend type Query {
  getPosts: [Post]
}
